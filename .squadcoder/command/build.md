---
description: Launch the app/web/electron development flow — plan, build, verify, CTO gate
agent: team
---

Run **Flow A — App / Web / Electron Development** for the request below.

Classify the scope first (state the flow + roster in one line), then orchestrate:
1. For greenfield/strategic asks, **CEO + CTO** set direction (vision, build-vs-reuse, quality bar).
2. **Product** locks scope + the MVP cut and success criteria.
3. **Architect** designs the system + produces the task breakdown.
4. **Multiple Devs in parallel** build the independent modules + **Frontend** on the UI (design system, RTL/Hebrew, accessibility).
5. **Security + QA** review.
6. **UI/UX Verifier** drives a real browser (Playwright) for full end-to-end verification.
7. Any errors route back **→ the CTO gate**, which signs off or sends Devs the fix list — **loop until green**.

Use the right skills/MCPs per stack (web, electron, backend, mobile). Keep token discipline: fire only the roles the task needs; the Researcher stays opt-in.

Request: $ARGUMENTS
